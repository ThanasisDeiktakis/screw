// ─── Client config ─────────────────────────────────────────────────────
// Edit this file on deploy. Other JS files should not be modified.

const SCREW_CONFIG = {
  // Spot URL. Change to your domain on deploy:
  // SERVER_URL: 'https://spot.example.com'
  SERVER_URL: 'http://localhost:8080',
};

